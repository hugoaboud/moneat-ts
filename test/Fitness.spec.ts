import { Genome } from "../src/Genome";
import { Genome as GenomeConfig } from "./config";

describe('Reset', () => {

    test.skip('Should create a population of configured size', async () => {
        
    });

})