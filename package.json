{
  "name": "moneat-ts",
  "version": "0.0.5",
  "description": "Multi-Object NeuroEvolution of Augmented Topologies",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "rm -rf build/ && tsc",
    "cli": "yarn build && node build/cli.js",

    "test": "yarn build && node --trace-warnings node_modules/.bin/jest --runInBand --no-cache --verbose --silent",

    "benchmark:iter": "yarn build && node build/benchmark/IterationBenchmark",
    "benchmark:network": "yarn build && node build/benchmark/NetworkBenchmark"
  },
  "keywords": [
    "neuralnetwork",
    "genetic",
    "multiobjective"
  ],
  "author": "@hugoaboud",
  "license": "MIT",
  "dependencies": {
    "@types/jest": "^27.0.3",
    "@types/node": "^17.0.2",
    "typescript": "^4.5.4"
  },
  "devDependencies": {
    "jest": "^27.4.5",
    "ts-jest": "^27.1.2"
  }
}
